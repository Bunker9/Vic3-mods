# =============================================================================
# MyDiploPlayMod — grant the aggressor a CLAIM ("core") on the target's state at
# game SETUP, so its start-of-game return_state war goal is valid (return_state needs
# a pre-existing claim). First instance: Punjab (c:PAN) claim on Sindh's only state.
# Additive state-history block (no create_state) — vanilla creates STATE_SINDH first
# (owner c:SIN); this appends the claim + homeland for low-unrest incorporation.
# (Design rationale for setup-vs-event lives in hk design notes.)
# =============================================================================
STATES = {
	s:STATE_SINDH = {
		add_claim = c:PAN          # the "core" -> enables low-infamy return_state
		add_homeland = cu:panjabi  # easy incorporation + low post-conquest unrest
		debug_log = diplo_claim_row   # commented out for master; -> "Claims & cores" table
		debug_log = diplo_core_row
	}
}
