# =============================================================================
# MyDiploPlayMod — fire the Punjab→Sindh war setup on game start.
# Additive on_actions sub-list pattern (own sub-action name, merges with vanilla
# and with Top40EcoBoostMod's startup without conflict).
# on_actions do NOT hotload — changes need a return to main menu.
# =============================================================================

on_game_started_after_lobby = {
	on_actions = { diplo_test_startup }
}

diplo_test_startup = {
	effect = {
		c:PAN ?= { trigger_event = { id = diplo_test.1 } }
	}
}
