namespace = mdp

# =============================================================================
# mdp.1 — monthly blob-chain driver. Fired per at-peace expand tag.
# Root = the expanding country.
# =============================================================================
mdp.1 = {
    type = country_event
    hidden = yes

    immediate = {
        debug_log = "[mdp.1] FIRED — country: [THIS.GetName] ([THIS.GetTag]) infamy=[THIS.GetInfamy]"

        save_scope_as = mdp_attacker   # MUST be saved here before mdp_try_next_war runs

        debug_log = "[mdp.1] scope:mdp_attacker saved — calling mdp_try_next_war"
        mdp_try_next_war = yes
        debug_log = "[mdp.1] mdp_try_next_war returned — [THIS.GetName] done for this pulse"
    }
}