namespace = mdp

# =============================================================================
# MyDiploPlayMod — generic blob-chain driver (PARKED 2026-06-12, pending rework).
# mdp.1 fired monthly per at-peace expander to pick its next conquest. Disabled along with
# mdp_try_next_war (see common/scripted_effects/mdp_effects.txt) until the neighbour-pick and
# runtime claim/pop logic is rebuilt. Kept as a stub so the namespace/file exists.
# =============================================================================
mdp.1 = {
	type = country_event
	hidden = yes

	immediate = {
		mdp_try_next_war = yes   # currently a no-op (parked)
	}
}
