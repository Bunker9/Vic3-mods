# =============================================================================
# Top40EcoBoostMod — Mod 1 economic-boost modifiers (named, applied via add_modifier)
# Champ = throughput (good-specific token) ; SoL via state_standard_of_living_add.
# (No clean per-building "wage ×N" token exists in Vic3 — wages rise from the
#  throughput-driven revenue; SoL is set explicitly here. Flavour gets no modifier yet.)
# =============================================================================

# CHAMP — country-scoped: floods the champ good (Punjab = opium). Phase 1 only (20 yrs).
mod1_champ_boost = {
	building_opium_plantation_throughput_add = 2.0
}

# SoL — state-scoped, Phase 1 (0–20 yrs): +5 standard of living (social-mobility push).
mod1_state_sol_p1 = {
	state_standard_of_living_add = 5
}

# SoL — state-scoped, Phase 2 (20 yrs → game end): +8 standard of living (permanent).
mod1_state_sol_p2 = {
	state_standard_of_living_add = 8
}
