# Vic3-mods

Monorepo for Victoria 3 mods. Each mod lives in its own subfolder with its own scaffold (`.metadata/metadata.json`, `common/`, `events/`, `localization/`).


## Adding a mod
Create a subfolder and scaffold it, e.g. `my-mod/.metadata/metadata.json` plus the game folders it needs.
