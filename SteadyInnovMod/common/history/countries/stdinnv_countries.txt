COUNTRIES = {
	# Austria: lower liberty desire across subjects for 50 years,
	# helping Crown Lands drift into annexable range.
	c:AUS ?= {
		add_modifier = {
			name = aus_crownland_management_50y
			years = 50
		}
	}

	# Hungary: directly lower its own liberty desire for 50 years,
	# making annexation by Austria more likely once it is a Crown Land.
	c:HUN ?= {
		add_modifier = {
			name = hungary_reconciled_to_vienna_50y
			years = 50
		}
	}

	# =========================================================================
	# SteadyInnov startup innovation tiers (2026-07-09) - MOVED here from the
	# stdinnv_startup on_action, which stamped stdinnv_subject on EVERY subject
	# worldwide (design intent was only to slow the big Asian economies). These
	# are permanent (no duration), cleared only via the mod's decisions.
	# Lists curated with the user from the SaveParse extract + EcoBoost targets
	# (see hk-config PROGRESS 2026-07-09). `?=` makes each a safe no-op if absent.
	# =========================================================================

	# GP boost - the four struggling great powers.
	c:FRA ?= { add_modifier = { name = stdinnv_boost_gp } }
	c:JAP ?= { add_modifier = { name = stdinnv_boost_gp } }
	c:NET ?= { add_modifier = { name = stdinnv_boost_gp } }
	c:USA ?= { add_modifier = { name = stdinnv_boost_gp } }

	# Subject penalty - ONLY the big Asian economies (India / China / Dutch East Indies).
	c:BIC ?= { add_modifier = { name = stdinnv_subject } }
	c:CHI ?= { add_modifier = { name = stdinnv_subject } }
	c:DEI ?= { add_modifier = { name = stdinnv_subject } }

	# Unrecognized penalty - major/minor unrecognized powers (save-survivors >3M + day-1
	# majors conquered in the save) + TIB/KOR (subjects, but the SOFT penalty) + EcoBoost
	# unrecognized targets (EGY/HYD/OMA/SHW).
	c:SOK ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:SIA ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:BUR ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:MAD ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:PER ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:AFG ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:MOR ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:DAI ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:NEP ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:PAN ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:TIB ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:KOR ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:EGY ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:HYD ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:OMA ?= { add_modifier = { name = stdinnv_unrecognized } }
	c:SHW ?= { add_modifier = { name = stdinnv_unrecognized } }

	# Montenegro: the Concordat exists from day one and holds the foreign gold
	# and paper set up in zz_stdinnv_mon_company.txt.
	# stock_exchange is in no starting tech tier; MON has its prerequisite
	# international_trade from the tier 3 bundle.
	c:MON ?= {
		add_technology_researched = stock_exchange
		add_technology_researched = corporate_charters
		add_modifier = { name = stdinnv_count_of_montenegro }
		add_modifier = { name = stdinnv_counts_greed years = 5 }
		add_company = company_type:company_lovcen_concordat
		company:company_lovcen_concordat = {
			set_company_establishment_date = 1463.1.26
			set_company_state_region = s:STATE_MONTENEGRO
		}
		activate_law = law_type:law_laissez_faire

		# The Count. Defined in common/character_templates/stdinnv_characters.txt,
		# which is where the company binding has to live.
		create_character = {
			template = stdinnv_lovcen_count
		}
	}
}
