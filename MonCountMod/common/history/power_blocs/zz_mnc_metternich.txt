# MON joins the Metternich System and the bloc gains market unification 2, which sets
# power_bloc_customs_union_bool. MON shares the Austrian market and stays independent.
# Tier 2 without tier 1 is vanilla-shaped: GBR adds principle_colonial_offices_2 the same way.
# Runs after vanilla 00_power_blocs.txt creates the bloc (zz_ load order).

POWER_BLOCS = {
	c:MON ?= {
		join_power_bloc = c:AUS
	}

	c:AUS ?= {
		power_bloc ?= {
			add_principle = principle_market_unification_2
		}
	}
}
